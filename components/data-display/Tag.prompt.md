Removable chip for filters and selected values.

```jsx
import { Tag } from "./Tag";

<Tag onRemove={() => remove("react")}>React</Tag>
<Tag leftIcon={<HashIcon />}>design-system</Tag>
```

Props: `onRemove` (shows the × button), `leftIcon`.
