# Secret used to sign the session JWT. Generate a strong one for any real use:
#   openssl rand -base64 32
# If unset, the app falls back to a built-in DEV-ONLY secret so `npm run dev` just works.
AUTH_SECRET="dev-only-insecure-secret-change-me-please-0123456789abcdef"
